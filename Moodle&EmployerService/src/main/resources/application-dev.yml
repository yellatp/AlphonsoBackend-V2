server:
  port: 8082

spring:
  application:
    name: moodle-service

  datasource:
    url: jdbc:mysql://localhost:3306/moodle_service_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: Root@123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

task:
  scheduling:
    pool:
      size: 2

# ✅ Moodle Configuration
moodle:
  base-url: http://54.243.88.200
  token: 778c601fe5af8b2996b3109bd7927672
  format: json
  default-role-id: 5

management:
  endpoints:
    web:
      exposure:
        include: "*"

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true

  instance:
    prefer-ip-address: true
